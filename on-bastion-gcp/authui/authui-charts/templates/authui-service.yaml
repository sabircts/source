apiVersion: v1
kind: Service
metadata:
  name: authui-service-np
  namespace: flowsource
spec:
  type: ClusterIP
  ports:
  - port: 32098
    protocol: TCP
    targetPort: 8080
  selector:
    app: authui